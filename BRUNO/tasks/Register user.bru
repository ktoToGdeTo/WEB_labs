meta {
  name: Register user
  type: http
  seq: 8
}

post {
  url: {{base-url}}/users/register
  body: json
  auth: inherit
}

body:json {
  {
    "username":"userrr",
    "password":"password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
